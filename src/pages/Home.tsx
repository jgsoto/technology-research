export const Home = () => {
    return (
        <section className="stack">
            <div>
                <p className="muted">Project 5 — TanStack Router Demo</p>
                <h1>Home</h1>
            </div>

            <p>
                This demo keeps the project intentionally small while showing typed navigation and a
                minimal external theme store powered by <code>useSyncExternalStore</code>.
            </p>
        </section>
    );
};